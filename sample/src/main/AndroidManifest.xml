<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidpi.layoutbinder.sample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NormalExampleActivity"
            android:label="@string/title_default_example" />
        <activity
            android:name=".DataBindingExampleActivity"
            android:label="@string/title_data_binding_example" />
        <activity android:name=".KotlinDataBindingActivity"
            android:label="@string/title_data_binding_example_in_kotlin"></activity>
        <activity android:name=".customview.CustomViewActivity"/>
    </application>

</manifest>